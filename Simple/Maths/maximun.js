/**
 * @param {number} a
 * @param {number} b
 * @return {number}
 */
var maximum = function(a, b) {
  return Math.max(a, b)
};


// var maximum = function(a, b) {
  // return (a+b)/2 + Math.abs(a-(a+b)/2);
// };
