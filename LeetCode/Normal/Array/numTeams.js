/**
 * @param {number[]} rating
 * @return {number}
 */
var numTeams = function(rating) {
    for (let i = 0; i < rating.length; i++) {
        for (let j = i + 1; j < rating.length; j++) {
        }
    }
};


numTeams([2,5,3,4,1]);
