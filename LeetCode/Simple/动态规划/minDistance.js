// 给你两个单词 word1 和 word2，请你计算出将 word1 转换成 word2 所使用的最少操作数 。

// 你可以对一个单词进行如下三种操作：

// 1. 插入一个字符
// 2. 删除一个字符
// 3. 替换一个字符

// 框架思路
// if s1[i] == s2[j]:
  // 啥都别做（skip）
  // i, j 同时向前移动
// else:
  // 三选一：
    // 插入（insert）
    // 删除（delete）
    // 替换（replace）

/**
 * @param {string} word1
 * @param {string} word2
 * @return {number}
 */
var minDistance = function(word1, word2) {

};
